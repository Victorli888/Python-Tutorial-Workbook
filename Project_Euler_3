def prime(n):
    factors = []

    # Creates a list of factors for n
    for i in range(1, n + 1):
        if n % i == 0:
            factors.append(i)

   # Removes an element from factors list if divisible by 2, 3, or 5.
   for x in factors:
        if (x % 2 == 0 or x % 3 == 0 or x % 5 == 0):
            factors.remove(x)

    return factors

print(prime(100))
